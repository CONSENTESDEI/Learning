"""
特点：
    1. 不可修改，只能查询
    2. 可重复
    3. 有序

基本操作：
    1. 创建：
        元组名 = (元素1, 元素2...)
        单个元素元组 元组名 = (元素,) 注意加 , 号
        空元组 元组名 = ()
    2. 方法
        count() |   统计某元素出现次数
        index() |   返回该元素第一次出现的位置
    3. 索引访问：直接打印
    4. 切片：与其他切片同理
"""

























